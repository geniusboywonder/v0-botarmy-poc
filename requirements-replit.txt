# Minimal requirements for stable Replit deployment
# Removes problematic ControlFlow/Prefect dependencies

# Core FastAPI and server dependencies
fastapi==0.116.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6
python-dotenv>=1.0.0

# Data validation and settings
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.2.1

# Logging
python-json-logger==2.0.7

# LLM providers (stable versions)
openai>=1.0.0
google-generativeai==0.5.4

# HTTP and file operations
httpx>=0.24.0
aiofiles>=23.0.0
requests>=2.31.0

# Essential utilities
typing-extensions>=4.0.0
python-dateutil>=2.8.0
orjson>=3.9.0
click>=8.0.0
rich>=13.0.0

# Security
cryptography>=41.0.0

# Optional performance enhancements
uvloop>=0.17.0; sys_platform != "win32"

# Development tools (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# REMOVED for Replit compatibility:
# controlflow>=0.11.0  # Causes import errors with Prefect
# prefect>=3.0.0       # Compatibility issues in Replit environment
# anthropic>=0.8.0     # Not needed for current implementation