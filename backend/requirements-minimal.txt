# Alternative requirements without ControlFlow for testing
# Use this if ControlFlow installation fails

# Core FastAPI and server dependencies
fastapi==0.116.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6

# Data validation and settings
pydantic==2.5.0
pydantic-settings>=2.2.1

# Logging
python-json-logger==2.0.7

# LLM integration only
google-generativeai==0.5.4

# Additional dependencies
httpx>=0.24.0
rich>=13.0.0
typing-extensions>=4.0.0
click>=8.0.0
aiofiles>=23.0.0

# Note: This will NOT include ControlFlow/Prefect orchestration
# The backend will need to be modified to work without these dependencies