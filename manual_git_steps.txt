#!/bin/bash

# Manual Git Operations Log for BotArmy Code Review
echo "=== BotArmy Manual Git Operations ==="
echo "Date: $(date)"

# Step-by-step git operations to safely backup and switch branches
echo "Please run these commands manually:"
echo ""
echo "1. Navigate to project directory:"
echo "   cd '/Users/neill/Documents/AI Code/Projects/v0-botarmy-poc'"
echo ""
echo "2. Check current status:"
echo "   git status"
echo "   git branch --show-current"
echo ""
echo "3. Stash any changes if needed:"
echo "   git stash push -m 'Auto-stash before process-refactor review'"
echo ""
echo "4. Create backup branch:"
echo "   git checkout -b backup-main-$(date +%Y%m%d-%H%M%S)"
echo "   git checkout main  # or whatever your current branch is"
echo ""
echo "5. Fetch remote changes:"
echo "   git fetch origin --prune"
echo ""
echo "6. Check if feature branch exists:"
echo "   git branch -r | grep feature/process-view-refactor-1"
echo ""
echo "7. Switch to feature branch:"
echo "   git checkout -b feature/process-view-refactor-1 origin/feature/process-view-refactor-1"
echo "   # OR if local branch exists:"
echo "   # git checkout feature/process-view-refactor-1 && git pull origin feature/process-view-refactor-1"
echo ""
echo "8. Verify the switch:"
echo "   git branch --show-current"
echo "   git log --oneline -5"
echo ""
echo "=== After running these commands, report back the results ==="
