# Production requirements for Vercel deployment
# This file is optimized to stay under the 250MB Vercel limit

# Core FastAPI and server dependencies - REQUIRED
fastapi==0.116.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6
python-dotenv>=1.0.0

# Data validation and settings - REQUIRED  
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.2.1

# Logging - REQUIRED
python-json-logger==2.0.7

# LLM integration - REQUIRED
google-generativeai==0.5.4

# HTTP client - REQUIRED
httpx>=0.24.0

# File operations - REQUIRED  
aiofiles>=23.0.0

# Essential utilities - REQUIRED
typing-extensions>=4.0.0
click>=8.0.0

# NOTE: Heavy dependencies (Prefect, ControlFlow) are conditionally imported
# They are only used in development mode when VERCEL environment variable is not set
# This keeps the Vercel deployment under the 250MB limit while maintaining full functionality locally